{
  "name": "yd-sdk",
  "version": "1.1.27",
  "description": "Typed isomorphic Yandex Disk SDK",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npx esbuild index.ts --bundle --outdir=dist --platform=neutral",
    "build-schema": "npx reqsrv-sdk --ns=YD --error=src/types/entities/Error.ts",
    "prepublishOnly": "npm run build",
    "preversion": "npx npm-run-all typecheck shape build",
    "shape": "npx codeshape",
    "typecheck": "tsc --noEmit"
  },
  "author": "axtk",
  "license": "MIT",
  "keywords": [
    "yandex-disk",
    "api",
    "typed api",
    "sdk",
    "typed sdk"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axtk/yd-sdk.git"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@t8/sdk-factory": "^1.0.1"
  }
}
